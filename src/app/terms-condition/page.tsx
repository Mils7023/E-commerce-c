"use client";

import { TermConditionContainer } from "@/container/TermConditionContainer";

const TermsConditionPage = () => {
  // You can fetch data based on the categorySlug here if needed
  // Or pass it down to the ProductListingContainer
  return <TermConditionContainer />;
};

export default TermsConditionPage;
